# LLM Support

I created this repository following Playwright's documentation; however, I also received help from Copilot, which suggested locators and functions when I started working on my component and action classes, and I follow the suggestions, checking first the suggestions really work and they would fit with what I had in mind and with the testing strategy, so for them I'm also did also corrections there.

I followed these instructions, both in Gemini and Copilot, to:

- Improve the README file (updating the file):

```
Please check and suggest corrections for this readme with the correct technical words, and also taking into account it will clear for people with basic technical skills. This is a framework's implementation in order to test a we page using Playwright. So there will be enoguh data to understand how the implementation works, requirements to execute it, the structure, architecture, steps to contribute there, how execute or create tests and others.
```

- Find the best option for implementing some actions on the website:

```
I have a filter on a website that I am not able to capture the options in the developer tools, within the browser itself. How can I get it from Playwright?
```

- Integrate with GitHub Actions, since I have limited experience with it:

```
How I could show on github the HTML report generated by Playwright on github actions?
```

- Create new test cases, we are able to use LLM in order to add new test cases also:

```
Given ./tests/ui/catalog.spec.ts file, add a test on catalog page that filter by proce (high to low) and select the first one t-shirt in the filter applied
```
